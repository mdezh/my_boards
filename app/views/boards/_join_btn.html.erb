<%= button_to join_board_path(board), id: 'join_board',
  class: "btn btn-light mt-2 w-100 shadow #{belongs ? 'hidden' : ''}",
  method: 'put', form: { data: {
    type: 'turbo_stream',
  }}, data: {
    controller: 'hide',
    hide_use_value: 'user_board_state',
    hide_check_value: 'owned || joined'
  } do %>
  <strong>Join</strong>
<% end %>

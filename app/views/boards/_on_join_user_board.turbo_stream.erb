<%= turbo_stream.replace 'user_board_state' do %>
  <%= render 'shared/state', id: 'user_board_state', value: {
    belongs: true,
    owned: false,
    joined: true
  } %>
<% end %>

<%= turbo_stream.update 'join_board', html: '' %>

<% if board.public_rw? %>
  <%= turbo_stream.replace 'add_note', partial: 'notes/add_form', locals: { board: } %>
<% end %>

<turbo-frame id="<%= dom_id(@room) %>">
  <%= form_with(model: @room) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.submit "Submit" %>
  <% end %>
</turbo-frame>

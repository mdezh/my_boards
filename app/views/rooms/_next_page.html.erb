<% if @more_pages %>
  <%= turbo_frame_tag(
    "rooms_page_#{@next_cursor}",
    autoscroll: true,
    loading: :lazy,
    src: rooms_path(cursor: @next_cursor)
  ) do %>
    Loading...
  <% end %>
<% end %>
